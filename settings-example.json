// This is an example for the config file which must be named "settings.json"
{
    "host": "0.0.0.0:2443  (ip + port the broker will listen)",
    "master": "127.0.0.1 (ip you expect the master will make requests from)",
    "mysql": {
        "host": "127.0.0.1 (MySQL ip)",
        "user": "web (MySQL username)",
        "pw": "SuPerPassword (MySQL password)",
        "db": "rmote (database name)"
    }
}